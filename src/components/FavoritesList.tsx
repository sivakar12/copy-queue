export default function({ onSelect }: { onSelect: (path: String) => {}} ) {
    // TODO: take from config
    const favorites = [
        '/Users/sivakar/Downloads',
        '/Users/sivakar/Desktop',
    ]

    return (
        <div>

        </div>
    )
}